.DS_Store
.omx/
.omc/
.playwright-cli/
__pycache__/
*.pyc

generated/
runtime/
unpacked/

edited.hwpx
result.hwpx
document.hwpx

# Secrets
.env
.env.*
*.env
# ...but templates (each version's server/.env.example) must stay trackable —
# without this, ".env.*" above silently blocks them from ever being committed.
!.env.example

# Dependencies
node_modules/

# polaris_dvc external clone (built via v3/scripts/setup-polaris-dvc.sh)
polaris_dvc/
v4/data/
v5/data/

# Claude Code (worktrees, projects, plugin caches)
.claude/
