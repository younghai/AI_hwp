{
  "name": "calendar-app-v5",
  "private": true,
  "type": "module",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace server\" \"npm run dev --workspace client\"",
    "dev:auto": "concurrently \"PORT=8795 CLIENT_ORIGIN=http://127.0.0.1:5195 OAUTH_REDIRECT_BASE=http://127.0.0.1:8795 npm run dev --workspace server\" \"VITE_SERVER_PORT=8795 VITE_AUTO_LOGIN=true npm run dev --workspace client -- --port 5195\"",
    "dev:client": "npm run dev --workspace client",
    "dev:server": "npm run dev --workspace server",
    "build": "npm run build --workspace client",
    "postinstall": "bash scripts/setup-rhwp-symlink.sh"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "dependencies": {
    "cookie-parser": "^1.4.7"
  }
}
