{
  "name": "ai-hwp",
  "private": true,
  "type": "module",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"PORT=8792 npm run dev --workspace server\" \"npm run dev --workspace client\"",
    "dev:auto": "concurrently \"PORT=8793 CLIENT_ORIGIN=http://127.0.0.1:5193 OAUTH_REDIRECT_BASE=http://127.0.0.1:8793 npm run dev --workspace server\" \"VITE_SERVER_PORT=8793 VITE_AUTO_LOGIN=true npm run dev --workspace client -- --port 5193\"",
    "dev:client": "npm run dev --workspace client",
    "dev:server": "npm run dev --workspace server",
    "build": "npm run build --workspace client",
    "lint": "npm run lint --workspace client",
    "test": "npm run test --workspace client",
    "postinstall": "bash scripts/setup-rhwp-symlink.sh"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
