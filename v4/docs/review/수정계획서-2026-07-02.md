# AI HWP — 수정 계획서 (Improvement Plan)

- **일자**: 2026-07-02
- **기반**: [역할별 검토보고서](./역할별-검토보고서-2026-07-02.md) — ID(BE/PY/FE/UX/PO-xx)는 해당 문서의 발견 항목과 1:1 대응
- **규모 표기**: S ≤ 0.5일 · M 1–2일 · L 3일+ (엔지니어 1인 기준)
- **역할**: BE(백엔드) · PY(파이썬) · FE(프론트) · DES(디자인) · PO

## 진행 현황 (2026-07-03 업데이트)

**✅ Phase 0 완료** — 커밋 `4e32c95`(파이프라인·서버·다크모드·pytest), `c724c26`(UI·브랜드·CI).

| 항목 | 상태 | 검증 근거 |
|---|---|---|
| 0-1 다이어그램 escape (PY-01) | ✅ | 런타임: `R&D`/`<`/`"` 포함 → well-formed SVG, 라이브 API에서 PNG 임베드 확인 |
| 0-2 NFC 정규화 (PY-02) | ✅ | NFD 헤딩 → 본문 정상 매핑(이전 빈 섹션) pytest 고정 |
| 0-3 원자적 쓰기 (PY-03) | ✅ | tmp+os.replace, 강제 kill 후 손상 zip 잔존 0, 실패 시 output 언링크 |
| 0-4 구조화 에러 (PY-04) | ✅ | 손상 업로드 → traceback/절대경로 미노출 JSON, 서버 로그엔 보존 |
| 0-5 다크모드 (UX-01) | ✅ | 모달 대비 1.2:1 → 12.35:1 실측, 라이트 18:1 유지 |
| 0-6 플로우 재배치 (UX-02) | ✅ | 업로더 step1, 조건 패널 디밍, 버튼 비활성 — 브라우저 확인 |
| 0-7 상태 뱃지 (UX-03) | ✅ | 'AI 키 필요 — 설정' 클릭 가능 텍스트 뱃지 |
| 0-8 진행 스텝퍼 (UX-04) | ✅ | 4단계 + 취소, 누락 CSS 정의, reduced-motion 대응 |
| 0-9 용량 사전검사 (FE-02) | ✅ | 클라 20MB 차단 + 서버 413 JSON |
| 0-10 ESLint/CI (FE-01) | ✅ | jsx-no-undef=error로 ErrorBoundary류 차단(scratch 검증), GitHub Actions |
| 0-11 pytest 스위트 | ✅ | 12개(R5/R6/NFC/escape/원자쓰기/zip-slip), checklist 편입 |
| 0-12 브랜드 통일 (UX-10) | ✅ | title/favicon/description |

**✅ Phase 2 완료** (2026-07-03) — 커밋 `535319a`(편집 루프·컨텍스트·슬롯·토큰), `c724c26`류 후속 5커밋.

| 항목 | 상태 | 검증 근거 |
|---|---|---|
| 2-1 초안 편집 루프 (PO-01) | ✅ | 생성/빌드 분리, 섹션 편집·추가·삭제·순서·재생성. 브라우저 E2E: 편집 본문이 다운로드 HWPX에 반영(E2E_LOOP_MARKER) |
| 2-2 원문 전체 컨텍스트 (PO-02) | ✅ | 3p 제한 제거→전 페이지(50p/12k자), 인접 중복만 제거 |
| 2-3 templateBodySlots (PO-03) | ✅ | estimateTemplateSlots로 헤딩 추정→payload 배선 |
| 2-4 모델 선택 + 실측 비용 (PO-05) | ✅ | 모델 선택기(Opus/Sonnet/Haiku), 기본 sonnet-5, localStorage 지속, payload 배선. 실측 usage 토큰 기반 비용 |
| 2-5 히스토리 (PO-07) | ✅ | /api/history + 접이식 패널, 재다운로드. 12건 확인 |
| 2-6 유형별 차별화 (PO-04) | ✅ | 유형별 지침 + 필드(회의록 일시/참석자, 제안서 수신처). payload 배선 확인 |
| 2-8 모드 배너 (PO-08) | ✅ | 업로드 직후 양식 유지/새 양식 배너 |
| 2-9 검증 언어화 (PO-09) | ✅ | '한컴에서 정상 열림' 요약 + 코드 상세 강등 |
| 2-10 스켈레톤/절단/성공 (UX-05/06/11) | ✅ | 스켈레톤 shimmer, 페이지 절단 표시, 성공 배지 |
| 2-7 Google 로그인 거취 | ⏸ | 유지 결정 → Phase 1 인증과 결합 예정 |

**부수 버그 수정**: `application/hwp+zip` MIME 미허용으로 샘플→빌드 실패(브라우저 E2E로 발견), test-provider가 새 `{text}` 형태 미사용.

**✅ Phase 1 완료** (2026-07-03) — 커밋 7개 (`harden(phase1)` 6 + 루프 수정 1).

| 항목 | 상태 | 검증 근거 |
|---|---|---|
| 1-1/1-2 인증 게이트 (BE-01/02) | ✅ | AUTH_MODE=local(무마찰)/protected. protected에서 generate/export/settings/generated 401, health 200 |
| 1-3 .env 원자화 (BE-03) | ✅ | mutex+tmp+rename, 동시 50write 100% 보존 |
| 1-4 산출물 격리 (BE-04/12) | ✅ | 결과 파일명 UUID, 업로드/sections 비공개 .work, /generated 인증 게이트 |
| 1-5 OAuth 토큰 분리 (BE-05) | ✅ | 인메모리 스토어(expiry+refresh), API 키 슬롯 미오염 |
| 1-6 rate limit/helmet (BE-06) | ✅ | 글로벌 1000·비용 30/15분(health 예외), 35연타→429. helmet 헤더 |
| 1-7 XSS escape (BE-07) | ✅ | OAuth error 페이로드 &lt;script&gt; 이스케이프 |
| 1-8 mock 프로덕션 차단 (BE-08) | ✅ | production에서 /auth/google/mock 404 |
| 1-9 쿠키 secure/CSRF (BE-15) | ✅ | secure(prod)+sameSite lax(CSRF 방어) |
| 1-10 runProcess (BE-10/11) | ✅ | spawn 실패 즉시 반환(hang 0), SIGKILL 폴백 |
| 1-11 zip-bomb (PY-05) | ✅ | 엔트리수/해제총량 상한, pytest |
| 1-12 defusedxml (PY-07) | ✅ | billion-laughs 거절 pytest |
| 1-13 .hwp OLE 매직 (BE-14) | ✅ | 위장 .hwp 415 |
| 1-14 generated TTL (BE-09) | ✅ | 24h/500MB 정리, node 검증 |

**부수 버그 수정**: rate limiter가 드러낸 useProviders 무한 fetch 루프(1000req/49s → 10초 1건). health 예외 + 글로벌 상한 상향.

**Phase 1 보안 종합 스모크**: protected 모드 8/8 통과(인증 401×4, health 200, mock 404, XSS escape, helmet).

---

## 실행 원칙

1. **Phase 0 → 1 → 2 → 3 순서 고정.** 로컬 사용자가 지금 겪는 무음 데이터 유실이 보안 하드닝보다 먼저다(현재 노출면은 127.0.0.1 뿐이므로).
2. 모든 수정은 기존 게이트(`hooks/pre-completion-checklist.sh` + `tools/smoke-test.sh`) 통과 후 완료 선언 — CLAUDE.md R1 준수.
3. Phase 0 완료 시점에 회귀 테스트(pytest + vitest + ESLint)를 함께 심어, 이후 Phase가 안전망 위에서 진행되게 한다.

---

## Phase 0 — 로컬에서도 깨지는 것: 무음 유실·온보딩 차단 제거 (예상 1주)

> 목표: "사용자가 넣은 내용이 조용히 사라지는" 모든 경로 제거 + 첫 실행 경험 정상화.

| # | 대응 ID | 작업 | 대상 파일 | 수용 기준 (Acceptance) | 규모 | 역할 |
|---|---|---|---|---|---|---|
| 0-1 | PY-01 | 다이어그램 SVG escape 도입 — `xml.sax.saxutils.escape` 헬퍼로 `title/step/label/date/a/b/header_*` 전부 감싸기 | `scripts/diagram_templates.py` | 본문·제목에 `R&D`, `<b>`, `"` 포함 시에도 다이어그램 PNG가 HWPX `BinData/`에 존재. 회귀 테스트: `"A & B"` → well-formed SVG | S | PY |
| 0-2 | PY-02 | sections JSON 로드 직후 heading/body **NFC 정규화** | `scripts/build_hwpx.py` (`load_sections_body`) | NFD 헤딩 입력 → 본문 정상 매핑(현재 빈 섹션). NFC/NFD 왕복 pytest 통과 | S | PY |
| 0-3 | PY-03 | `pack_hwpx` 원자적 쓰기(tmp + `os.replace`) + 실패 시 서버측 output 언링크 | `scripts/office/hwpx_utils.py`, `server/services/hwpxBuilder.js` | 빌드 중 SIGTERM에도 `/generated`에 손상 zip 잔존 0 (테스트: 강제 kill 후 디렉터리 검사) | S | PY |
| 0-4 | PY-04 | `main()` 예외 래핑 → `{"error_code","message"}` JSON(stdout) + exit code. Node는 stderr 원문 노출 중단, 코드→사용자 문구 매핑 | `scripts/build_hwpx.py`, `server/services/hwpxBuilder.js`, `server/lib/utils.js` | 어떤 실패에도 traceback·절대경로가 API 응답에 미포함. 손상 sections JSON은 무음 진행 대신 422 반환(PY-08 일부 해소) | M | PY+BE |
| 0-5 | UX-01 | 다크모드 판독 불가 해소 — 하드코딩 `#fff`/라이트 rgba 8곳을 `--surface`/`--surface-raised`/`--border-strong` 토큰으로 승격, 다크 값 정의 | `client/src/styles.css` | 다크모드에서 모달/토스트/카드 텍스트 대비 ≥ 4.5:1 (`preview_inspect`로 실측). 스코프 초과 시 임시 폴백: `color-scheme: light` 고정 | M | DES+FE |
| 0-6 | UX-02 | 정보 구조 재배치 — 업로더를 step 1 최상단, 생성 조건을 step 2로. 파일 없으면 조건 패널 디밍. 모바일에서 업로더 최우선 | `client/src/components/ControlPanel.jsx`, `styles.css` | 첫 화면(데스크톱·375px 모바일)에서 업로드 CTA가 스크롤 없이 보임. 패널 넘버링과 EmptyState 스텝 일치 | M | FE+DES |
| 0-7 | UX-03 | 미연결 상태를 텍스트 뱃지로 — "AI 키 필요 — 설정" 클릭 시 모달 오픈 | `client/src/components/TopBar.jsx` | 미연결 시 hover 없이 상태+행동이 보임 | S | FE |
| 0-8 | UX-04 | 진행 스텝퍼(분석→AI→빌드→렌더) + 취소 버튼(기존 AbortController 배선) + 누락 CSS(`.preview-process-line`, `.draft-section-meta`) 정의 | `client/src/components/PreviewPanel.jsx`, `App.jsx`, `styles.css` | 생성 중 현재 단계·경과가 보이고 취소가 동작(취소 후 재생성 정상) | M | FE |
| 0-9 | FE-02/UX-07 | 업로더 20MB 사전 검사 + multer 에러 JSON 핸들러 | `client/src/components/Uploader.jsx`, `server/routes/export.js` | 21MB 파일 → 즉시 클라이언트 reject 문구. 서버 우회 시에도 `{ok:false,error}` JSON 수신 | S | FE+BE |
| 0-10 | FE-01 | ESLint(no-undef 포함) + `client` test 스크립트 + GitHub Actions CI(lint→vitest→build→서버 syntax) | 루트/`client/package.json`, `.github/workflows/` | `pnpm lint`·`pnpm -C client test` 통과가 CI 게이트. ErrorBoundary류 미정의 참조가 CI에서 검출됨을 회귀 케이스로 확인 | M | FE |
| 0-11 | PY-09 | pytest 도입 + 핵심 회귀 스위트: R5 정규화 / R6 parity(섹션>슬롯·슬롯>섹션·0섹션) / NFC / escape / 손상 zip | `scripts/tests/` (신규), `requirements.txt` | `pytest scripts/tests` 그린. pre-completion-checklist에 편입 | M | PY |
| 0-12 | UX-10 | 브랜드 통일 — `<title>AI HWP`, favicon, 명칭 1본화("AI HWP — HWP/HWPX 문서 자동화") | `client/index.html`, `TopBar.jsx` | v2/Studio 혼용 표기 0 | S | DES |

**Phase 0 종료 기준**: smoke-test + pre-completion-checklist + 신규 pytest/vitest/ESLint 전부 그린, 다크모드 실측 대비 통과, `R&D` 포함 E2E에서 다이어그램 존재 확인.

---

## Phase 1 — 외부 배포 차단 해제: 보안 하드닝 (예상 1~1.5주)

> 목표: "localhost를 벗어나는 순간"의 P0 8건 제거. 로컬 전용으로 남길 거면 이 Phase는 축소 가능(1-1, 1-6, 1-7만 권장).

| # | 대응 ID | 작업 | 대상 파일 | 수용 기준 | 규모 | 역할 |
|---|---|---|---|---|---|---|
| 1-1 | BE-01 | `requireSession` 미들웨어 신설, 상태 변경·비용 발생 라우트 전체 적용(로컬 모드에선 자동 통과 옵션 `AUTH_MODE=local`) | `server/lib/session.js`, `index.js`, 각 라우트 | 미인증 POST가 401. 로컬 모드 기본값으로 기존 UX 무변화 | M | BE |
| 1-2 | BE-02 | `/api/settings` 보호 — 인증 필수 + 로컬 요청 검증, 배포 프로파일에서 비활성화 | `routes/providers.js` | 배포 모드에서 키 쓰기 404/403 | S | BE |
| 1-3 | BE-03 | `.env` 쓰기 직렬화(in-process mutex) + tmp+rename 원자화 | `lib/env.js` | 동시 쓰기 100회 스트레스에서 유실/파손 0 | S | BE |
| 1-4 | BE-04+12 | 산출물 파일명 `crypto.randomUUID()` + `/generated` 인증 뒤로 + 업로드 임시파일을 비공개 tmp로 분리 | `hwpxBuilder.js`, `index.js` | 파일명 추측 불가, 동시 export 교차 오염 불가(밀리초 충돌 소멸) | M | BE |
| 1-5 | BE-05 | OAuth 토큰을 API 키 슬롯과 분리 저장 + `expires_in`/`refresh_token` 처리 | `routes/auth.js`, `lib/env.js` | 토큰 만료 시 자동 갱신 or 명시적 재연결 안내 | M | B |
| 1-6 | BE-06 | `express-rate-limit`(AI/export/settings 라우트별) + `helmet` | `index.js` | 초당 폭주 요청이 429 | S | BE |
| 1-7 | BE-07 | OAuth/mock 결과 페이지 HTML escape 공통 함수 + 3벌 템플릿 통합 | `lib/oauth.js`, `routes/googleAuth.js`, `auth.js` | `?error=<script>` 반사 불가 | S | BE |
| 1-8 | BE-08 | mock 로그인 라우트를 `NODE_ENV!=='production' && !CLIENT_ID_CONFIGURED`일 때만 등록 | `routes/googleAuth.js` | 프로덕션에서 `/auth/google/mock` 404 | S | BE |
| 1-9 | BE-15 | 쿠키 `secure`(prod) + CSRF(double-submit or sameSite:strict+Origin 검증) | `googleAuth.js` 외 | 체크리스트 통과 | S | BE |
| 1-10 | BE-10/11 | `runProcess` — `child.on('error')` 처리 + SIGTERM→5s→SIGKILL 폴백 | `lib/utils.js` | python3 부재 시 즉시 503(무한 hang 0), 타임아웃 후 프로세스 잔존 0 | S | BE |
| 1-11 | PY-05 | zip 안전 추출 — 경로 검증 + 엔트리 수/해제 총량 상한(화이트리스트: `Contents/*`, `META-INF/*`, `mimetype`, `Preview/*`) | `scripts/office/hwpx_utils.py` | zip-slip/폭탄 픽스처가 구조화 에러로 거절 | M | PY |
| 1-12 | PY-07 | `defusedxml` 파싱 적용(읽기 경로만) | `build_hwpx.py`, `requirements.txt` | billion-laughs 픽스처 거절 | S | PY |
| 1-13 | BE-14 | `.hwp` OLE 매직(D0 CF 11 E0) 검증 + MIME 목록에서 `''`/octet-stream 제거 | `lib/upload.js` | 위장 바이너리 422 | S | BE |
| 1-14 | BE-09 | `generated/` TTL 청소(기본 24h, 기동 시+주기) + 총 용량 상한 | `hwpxBuilder.js` 또는 신규 `lib/cleanup.js` | 오래된 산출물 자동 삭제 확인 | S | BE |

**Phase 1 종료 기준**: 위 항목 완료 + 간단한 침투 체크(무인증 호출·XSS 페이로드·zip 픽스처) 스크립트 그린. 외부 배포 판정을 "가능"으로 승격.

---

## Phase 2 — 제품 가치: 핵심 루프 완성 (예상 2주, PO 결정 포함)

| # | 대응 ID | 작업 | 수용 기준 | 규모 | 역할 |
|---|---|---|---|---|---|
| 2-1 | PO-01 | **초안 편집 루프** — 섹션 본문 인라인 편집(textarea 전환), 섹션 단위 재생성 버튼, 목차 항목 추가/삭제/순서 변경. 편집본이 export payload에 그대로 반영 | 편집 → 다운로드 시 편집 내용이 HWPX에 존재(E2E) | L | FE+BE+PO |
| 2-2 | PO-02 | **원문 전체 컨텍스트** — rhwp 전 페이지 텍스트 추출로 확대(+상한: 문자 수 기준 청크/요약). `extractTextFromSvg`의 전역 중복 제거를 라인 인접 중복만으로 완화 | 10p 문서에서 후반부 고유 키워드가 AI 프롬프트에 포함됨(로그 확인) | M | FE |
| 2-3 | PO-03 | `templateBodySlots` 배선 — 업로드 분석 시 슬롯 수 산출(서버 or rhwp) → generate-draft payload에 포함 | 슬롯 8개 템플릿에서 AI 섹션 수가 슬롯 기준으로 유도됨 | M | FE+BE |
| 2-4 | PO-05 | 모델 선택 UI + 최신 모델 기본값 갱신 + **실제 API usage 토큰 기반** 비용 표시(`ai.js`가 usage 반환하도록) | 응답 토스트의 비용이 응답 usage와 일치 | M | BE+FE |
| 2-5 | PO-07 | 최근 생성 문서 목록(서버 `generated/` 인덱스 API + 클라이언트 패널, 재다운로드/미리보기) | 새로고침 후에도 과거 결과 접근 가능 | M | BE+FE |
| 2-6 | PO-04 | 문서 유형별 차별화 1차 — 유형별 프롬프트 지침·입력 필드(회의록: 일시/참석자, 제안서: 수신처) + 유형별 샘플 3종 추가 | 5유형 중 3유형 이상이 고유 필드·샘플 보유 | M | PO+FE |
| 2-7 | PO-06 | Google 로그인 거취 결정 — (a) 제거 or (b) 히스토리·키 개인화와 결합. **PO 결정 선행** | 결정 문서화 + 구현 | S~M | PO |
| 2-8 | PO-08 | HWP/HWPX 모드 배너 — 업로드 직후 "양식 유지" vs "새 양식 생성" 명시 | 사용자 테스트에서 모드 오해 0 | S | DES |
| 2-9 | PO-10 | 검증 패널 사용자 언어화 — 요약 우선("한컴에서 정상 열림 ✓"), 코드 상세는 접기 | 비개발자가 결과를 해석 가능 | S | DES |
| 2-10 | UX-05/06/11 | 스켈레톤 로딩(가짜 문장 제거), "미리보기 n/전체 N페이지" 표시, 완료 순간 성공 연출 | 각 항목 시각 확인 | M | DES+FE |

---

## Phase 3 — 품질 인프라·리팩토링 (지속)

**✅ Phase 3 완료** (2026-07-03) — 커밋 7개(polish/refactor/feat/test/harden phase3).

| 항목 | 상태 | 검증 근거 |
|---|---|---|
| 3-1 서버 테스트 (BE-17) | ✅ | vitest 16개(upload/env/metrics/oauth/cleanup/authGuard local·protected), CI/게이트 편입 |
| 3-2 구조화 로깅 (BE-18) | ✅ | pino JSON 로그 + reqId + /api/metrics(hwpx_build/ai_draft 지연·성패) |
| 3-3 스폰 세마포어 (BE-19) | ✅ | runProcess 동시성 상한(cores-2), 5×sleep MAX=2 직렬화 확인. polaris 미설치 스킵은 기존 구현 |
| 3-4 JSON 추출 견고화 (BE-13) | ✅ | 중괄호 균형 스캔(6케이스 테스트) + openai json_object 모드. 정규식 폴백 유지 |
| 3-5 죽은 코드 (BE-16) | ✅ | @anthropic-ai/sdk·App aiApiKey·_BODY_SENTENCES 제거 |
| 3-6 결정성 (PY-P2) | ✅ | --doc-date 주입(동일입력 동일출력 pytest), clone_form 도구 전용 명시 |
| 3-7 설정 중앙화 (BE-P2) | ✅ | config.js(포트 5188/8788↔5192/8792 불일치 해소), 쿠키 v2_session→ai_hwp_session |
| 3-8 디자인 폴리시 (UX-08/09/12) | ✅ | Pretendard+word-break, 모달 포커스 트랩, 토스트 hover 정지, --mono 토큰 |

**보류(문서화)**: 응답 엔벌로프 통일(클라 결합·회귀 위험), multer diskStorage(매직바이트 buffer 의존+rate-limit로 위험 제한적), mimetype STORED 3파일 통합·다중 sectionN.xml(회귀 위험, 별도 과제).

**주의**: 쿠키명 변경(v2_session→ai_hwp_session)으로 기존 로그인 세션은 무효화됨(pre-launch 무해).

---

### 원본 계획 (참고)

| # | 대응 ID | 작업 | 규모 |
|---|---|---|---|
| 3-1 | BE-17 | 서버 단위/통합 테스트(validate·upload·env·에러 경로, export 통합은 spawn 모킹) | M |
| 3-2 | BE-18 | `pino` 구조화 로깅 + 요청 ID + AI/스폰 지연·실패 메트릭(PO-09의 로컬 지표 겸용) | M |
| 3-3 | BE-19/20 | Python 워커 상주화(데몬+IPC) 또는 동시 스폰 세마포어, multer diskStorage 전환, polaris 미설치 시 스폰 스킵 | M~L |
| 3-4 | BE-13 | AI 응답을 provider 구조화 출력(tool use / response_format)으로 전환 — 정규식 JSON 추출 제거 | M |
| 3-5 | BE-16, FE-P2 | 죽은 코드 정리: `@anthropic-ai/sdk`, `App.jsx aiApiKey`, `sourceMode`, `_body_sentence`, `v2` 잔재(쿠키명·localStorage 키) 일괄 개명 | S |
| 3-6 | PY-P2 | `clone_form.py`를 도구 전용으로 명시(또는 R5 준수 재작성), mimetype STORED 로직 `hwpx_utils` 단일화, 매직넘버(용지 폭)를 `pagePr`에서 계산, `--doc-date` 주입으로 출력 결정성 확보, 다중 `sectionN.xml` 처리 | M |
| 3-7 | BE-P2 | 설정 중앙화 `config.js`(포트 기본값 5188/8788 vs 5192/8792 불일치 해소), API 응답 엔벌로프 `{ok,data,error}` 통일 | M |
| 3-8 | UX-08/09/12 | Pretendard 폰트 스택 + `word-break:keep-all` 전역, 모달 포커스 트랩, `prefers-reduced-motion`, 토스트 hover 정지, 디자인 토큰 정리 | M |

---

## 검증 계획

| 시점 | 게이트 |
|---|---|
| 매 PR | ESLint + vitest + pytest + 서버 `node --check` (CI, 0-10에서 구축) |
| 매 Phase 종료 | `bash hooks/pre-completion-checklist.sh` (프로덕션 빌드 + 6단계 smoke E2E 포함) |
| Phase 0 특화 | `R&D`/NFD 헤딩/강제 kill 픽스처 E2E, 다크모드 대비 실측(≥4.5:1) |
| Phase 1 특화 | 무인증 호출·XSS 페이로드·zip 픽스처 거절 스크립트 |
| Phase 2 특화 | 편집→다운로드 바이트 검증(`tools/verify-hwpx-markers.py`에 편집 마커 추가) |

## 리스크 및 의존성

- **rhwp 전 페이지 추출(2-2) 성능**: 대형 문서에서 WASM 렌더 비용 — 텍스트 전용 추출 API 유무 확인 필요, 없으면 서버측 XML 파싱으로 우회(Python이 이미 XML을 다루므로 저위험).
- **다크모드 정식 대응(0-5) 스코프 팽창**: 토큰 승격 중 시각 회귀 위험 — 화면별 스크린샷 비교로 통제, 여의치 않으면 `color-scheme: light` 고정 후 Phase 3로 이월.
- **초안 편집(2-1)은 상태 모델 변경**: draft 불변 가정이 깨짐 — `draftControllerRef` 취소 로직과 편집 중 재생성 충돌 케이스를 설계 단계에서 명세할 것.
- **인증 도입(1-1)과 로컬 UX 충돌**: `AUTH_MODE=local` 기본으로 로컬 무마찰 유지 — 배포 프로파일에서만 강제.

---

## 종합 보고 (배포 전 점검 기준 형식)

**반드시 수정해야 할 항목** — Phase 0 전체(특히 PY-01/02/03/04: 사용자 콘텐츠 무음 유실 3건 + traceback 노출, UX-01 다크모드 온보딩 차단). 외부 배포 의사가 있다면 Phase 1 전체가 추가로 "반드시"로 승격.

**수정하면 좋은 항목** — Phase 2 전체(초안 편집 루프, 전체 원문 컨텍스트, 모델 선택/실측 비용, 히스토리, 유형별 차별화), FE 에러 처리 보강(FE-03/04), 진행 스텝퍼(0-8).

**현재 상태로 유지해도 되는 항목** — R5/R6 파이프라인 핵심 로직, 업로드 다층 검증 설계, OAuth state/세션 난수 품질, 토스트+액션 패턴, EmptyState 샘플 온보딩, 반응형 레이아웃 골격, 검증기(container/structure) 설계.

**리팩토링 추천 항목** — Phase 3 전체(워커 상주화, 구조화 출력 전환, 설정 중앙화, 죽은 코드 제거, clone_form 단일화, 디자인 토큰 정리).

**최종 배포 가능 여부** — Phase 0/1/2 완료 (2026-07-03).
- **로컬 개인 도구**: **가능** — 기본값(`AUTH_MODE=local`)으로 무마찰 실행.
- **외부/팀 배포**: **가능(조건부)** — `AUTH_MODE=protected` + `NODE_ENV=production` + Google OAuth(`GOOGLE_CLIENT_ID/SECRET`) + HTTPS 프록시로 실행. Phase 1 차단 항목(무인증 API, 키 평문 쓰기, mock 우회, XSS, rate limit, zip)이 모두 해소됨. protected 보안 스모크 8/8 통과.
- **남은 배포 권장(비차단, Phase 3)**: 구조화 로깅/관측성(BE-18), 서버 통합 테스트(BE-17), Python 워커 상주화(BE-19), 다중 사용자 격리가 필요하면 히스토리/키의 per-user 스코프.

**개선 우선순위 (요약)**
1. PY-01 다이어그램 escape (S — 콘텐츠 무음 유실, 반나절)
2. PY-02 NFC 정규화 (S — macOS 본문 유실)
3. PY-03/04 원자적 쓰기 + 구조화 에러 (S+M)
4. UX-01 다크모드 (M — 온보딩 차단)
5. UX-02/03 플로우 순서 + 상태 노출 (M+S — 첫인상)
6. FE-01/PY-09 CI + 테스트 안전망 (M+M)
7. (배포 시) Phase 1 보안 일괄
8. PO-01/02 편집 루프 + 전체 컨텍스트 (L+M — 제품 승격의 갈림길)
